# Dev admin access (local notes)

Any notes about temporary/dev admin passwords belong in a **local-only** file named `SUPABASE_DEV_ADMIN_PASSWORD.md` (gitignored).

Use a strong unique password in production. Never commit real passwords. Prefer env vars / secret stores.
